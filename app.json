{
  "name": "Tzunami",
  "logo": "https://telegra.ph/file/050540d246273c5daaa94.jpg",
  "description": "A Powerful Telethon Based Virus-Spambot.",
  "keywords": ["virus", "spambot", "telegram"],
  "repository": "https://github.com/Zycho-Dev-66/Tzunami",
  "stack": "container",
  "buildpacks": [
   {
     "url": "heroku/python"
   }   
  ],        
  "env": {
    "API_ID": {
      "description": "Get the API ID from my.telegram.org/apps",
      "required": true
    },
    "API_HASH": {
      "description": "Get the API Hash from my.telegram.org/apps",
      "required": true
    },
    "SESSION": {
      "description": "Get the telethon session from running session.py locally or run the https://replit.com/@ZychoDev/String-Session",
      "required": true
    },    
    "ALIVE_NAME": {
      "description": "Your name",
      "required": true
    },
    "COMMAND_HANDLER": {
      "description": "Set a back slash (\) before the handler",
      "required": true,
      "value": "."
    },
    "ALIVE_PIC": {
      "description": "Set a custom pic for the alive module",
      "required": false
    },
    "FILTERS": {
      "description": "Change to disable the auto chat filters",
      "required": false,
      "value": "True"
    },
    "LOGGER_ID": {
      "description": "Create a private group add @MissRose_bot , send /id and paste the id here.",
      "required": false
    }
  }
}
